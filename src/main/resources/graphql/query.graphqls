scalar Date
scalar BigDecimal

type Query {
    movies(keyword: String!, page: Int, includeAdult: Boolean, region: String, year: Int, primaryReleaseYear: Int): SearchResponse!
    movieById(id: ID!): Movie
}
